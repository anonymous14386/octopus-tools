# octopus-tools

Static browser utilities — hash generator, base converter, password generator
and speed reader.

Plain HTML and JS in `public/`, served by nginx. No backend, no accounts, no
data leaves the browser.
